
- 2026-04-11: Task 1 scaffolding must preserve the execution-plan stack lock: Astro + TypeScript + static output + npm + MDX content collections + GA4 + Formspree + Vercel.
- 2026-04-11: Task 1 must create only the minimal baseline folders required by the plan: `src/pages`, `src/content`, `src/components`, `src/layouts`, `public`, `tests/e2e`.
- 2026-04-11: Task 1 must avoid undocumented additions such as Tailwind, React islands, CMS/auth/database/server runtime features, GTM, or custom backend routes.
- 2026-04-11: User clarified new product direction: the site includes an in-site video generation app, users can generate up to 2 free videos, and there is already an existing API to integrate with. `Q3=AA` is being interpreted as `Q3=A` unless corrected.
- 2026-04-11: User does not want Playwright as the browser QA path; future UI/browser verification should prefer the available browser-agent workflow instead.
- 2026-04-11: User further clarified the existing generation API can be called directly from the frontend (`B`); architecture revision should therefore treat server proxying as optional rather than assumed.
- 2026-04-11: Revised authority documents preserve Astro and static-first content delivery, adding only the smallest architecture change, a frontend-driven generator on `/try-happyhorse/` rather than a new backend platform.
- 2026-04-11: `/try-happyhorse/` is now the canonical app/access route and must explicitly cover generation start, success, failure, and the 2-free-generation limit in both implementation and QA planning.
- 2026-04-11: Task 1 keeps `/try-happyhorse/` app-ready without creating the route yet by reserving the directory path and calling it out from the placeholder homepage, leaving the actual app implementation to later tasks.
- 2026-04-11: Task 1 avoids a Playwright-only QA lock by routing `test:e2e` through `tests/e2e/run.mjs`, which can run a different browser command later via `E2E_COMMAND` while still supporting the current default runner.
- 2026-04-11: Task 3 uses one shared Astro page shell with `src/pages/index.astro` for the homepage and `src/pages/[...slug].astro` for all remaining content pages, keeping SEO metadata, JSON-LD, breadcrumbs, CTA logic, and uncertainty UI centralized instead of duplicating per route.
- 2026-04-11: Task 4 keeps the generator as a plain Astro component with inline client script instead of introducing React or extra routes, because the requested MVP only needs one isolated interactive island inside the existing shared shell.
- 2026-04-11: Task 4 treats the local 2-free-generation rule as UX gating only: successful counts persist in `localStorage`, submission is disabled while pending, and telemetry is emitted through lightweight `dataLayer`/`gtag`/`CustomEvent` hooks instead of a heavier analytics abstraction.
- 2026-04-11: Task 5 adds the minimum legal/privacy baseline as one standalone `/privacy-compliance/` route plus a shared-shell footer link, rather than turning the compliance copy into another content-collection entry, so the existing route inventory and page model stay intact.
- 2026-04-11: Final blocker repair should stay surgical: keep Astro’s generated `sitemap-index.xml`/`sitemap-0.xml`, add only a lightweight `/sitemap.xml` alias route, move the homepage disclaimer up within the shared shell, and harden generator link rendering without changing the route inventory or generator telemetry flow.
