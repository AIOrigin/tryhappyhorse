
- 2026-04-11: Workspace is pre-scaffold only. No `package.json`, `astro.config.*`, `src/`, `public/`, or `tests/` exist yet.
- 2026-04-11: MVP route authority comes from `.sisyphus/drafts/auto-seo-happyhorse-product-spec.md`, not the broader route list in `README.md`.
- 2026-04-11: Locked Must routes are `/`, `/what-is-happyhorse/`, `/happyhorse-vs-seedance/`, `/try-happyhorse/`, `/happyhorse-alternatives/`, `/happyhorse-prompts/`.
- 2026-04-11: Locked Should routes are `/happyhorse-github/`, `/happyhorse-open-source/`, `/best-ai-video-models/`.
- 2026-04-11: Forbidden standalone v1 routes include `/seedance-vs-happyhorse/`, `/happyhorse-api/`, `/happyhorse-hugging-face/`, `/happyhorse-alibaba/`, `/news/`.
- 2026-04-11: Local conventions to preserve: Chinese planning docs, English site copy, kebab-case routes, one shared page shell, one canonical comparison route, access intent consolidated into `/try-happyhorse/`.
- 2026-04-11: Astro official/public guidance supports a minimal static setup using Astro + TypeScript + `@astrojs/mdx` + `@astrojs/sitemap`, with `site` configured, content collections in `src/content.config.*`, and Playwright using `npm run preview` as the web server.
- 2026-04-11: Recommended baseline scripts from Astro guidance are `dev`, `build`, `preview`, `astro`, and `test:e2e`; `build` should include `astro check && astro build` because `astro build` alone does not type-check.
- 2026-04-11: Canonical generation and sitemap correctness depend on setting `site`; collections do not create routes automatically; static route verification should prefer preview/build over dev server.
- 2026-04-11: Revised source of truth keeps Astro, keeps the 6 Must + 3 Should route inventory, and re-scopes `/try-happyhorse/` from waitlist/access copy into the canonical in-site app/access entry.
- 2026-04-11: The updated plan/spec no longer treat lead capture as the sole primary conversion. Generation events now drive the primary product telemetry: `generation_start`, `generation_success`, `generation_failure`, and `generation_limit_reached`.
- 2026-04-11: Task 1 repo state was already partially scaffolded despite older notes saying it was empty; `package.json`, Astro config, `src/`, `public/`, `tests/`, and install artifacts already existed before foundation cleanup.
- 2026-04-11: A stable browser-test hook can stay runner-neutral by keeping `test:e2e` behind a small Node wrapper that starts `npm run preview`, waits for HTTP readiness, and then runs the browser command via `E2E_COMMAND`.
- 2026-04-11: The safer Task 1 interpretation of “do not hard-lock a Playwright-only browser QA path” is stronger than just making the script overridable; the baseline scaffold should not ship bundled Playwright dependencies, config, or spec files at all.
- 2026-04-11: A minimal `test:e2e` for the foundation task can smoke-check built output in `dist/` by default and still allow a future external browser runner through `E2E_COMMAND` without introducing framework-specific repo artifacts.
- 2026-04-11: Astro 6.1.5 content schemas should import `z` from `astro/zod`, not `astro:content` or `astro:schema`, if we want to avoid the current deprecation hints while keeping the same content-collection architecture.
- 2026-04-11: Task 2 locks the content inventory to exactly 9 MDX entries under `src/content/pages/`: home, what-is-happyhorse, happyhorse-vs-seedance, try-happyhorse, happyhorse-alternatives, happyhorse-prompts, happyhorse-github, happyhorse-open-source, and best-ai-video-models.
- 2026-04-11: The typed page collection now bakes in route metadata, source URLs, fact-status notes, CTA enums, related pages, and generation-entry mode so later page-shell work can render from content data without widening the route set.
- 2026-04-11: Astro 6 content collections backed by local MDX files need an explicit `glob` loader in `src/content.config.ts`; schema types can still generate even while the runtime content store stays empty.
- 2026-04-11: Shared-shell QA should verify both `/` and `/try-happyhorse/`: the homepage confirms `mixed` uncertainty treatment and shared CTA wiring, while `/try-happyhorse/` confirms `unknown` treatment plus canonical, Open Graph, breadcrumb, FAQ JSON-LD, related-links, and disclaimer output.
- 2026-04-11: Task 4’s safest insertion point is `src/layouts/PageShell.astro`: replace the CTA block only for `page.data.route.is_canonical_app_entry`, leaving the rest of the shared shell untouched.
- 2026-04-11: Existing CTA routing in `src/lib/page-shell.ts` should keep sending other pages into `/try-happyhorse/`; only the canonical route itself should swap from placeholder CTA to live generator UI.
- 2026-04-11: The least fragile MVP quota model is in-memory UI state (`ready|submitting|success|failure|limit_reached`) plus `localStorage` persistence of successful generation count only, clamped to 0..2.
- 2026-04-11: Count a generation only after a usable successful API response for the latest active attempt; avoid double-counting with a per-submit attempt id and disabled submit state.
- 2026-04-11: Task 4 now mounts a dedicated `GeneratorEntry.astro` component only on `/try-happyhorse/`, leaving all other shared-shell CTA routing and SEO/disclaimer/fact-status/breadcrumb/related-links output unchanged.
- 2026-04-11: The repo still lacks a concrete frontend-safe generation endpoint, so the MVP uses `PUBLIC_HAPPYHORSE_GENERATOR_ENDPOINT` as a minimal direct-call integration surface and falls back to an explicit local mock mode for honest success/failure/limit QA.
- 2026-04-11: The second successful generation transitions the UI straight into `limit_reached` while still preserving the latest successful result details, which keeps both the terminal success accounting and the free-limit messaging visible.
- 2026-04-11: Task 5 can add the legal baseline without widening the content collection by using a standalone `src/pages/privacy-compliance.astro` page that still imports `buildCanonicalUrl` from `src/lib/seo.ts`, which preserves the shared canonical derivation path.
- 2026-04-11: Astro static preview serves `src/pages/robots.txt.ts` cleanly alongside `@astrojs/sitemap`; the generated `robots.txt` can allow all public crawling and point at `https://tryhappyhorse.xyz/sitemap-index.xml` while the sitemap payload stays in `sitemap-0.xml` behind that index.
- 2026-04-11: Task 6 homepage refinement can stay inside the shared shell by treating `src/content/pages/home.mdx` as the hub source of truth: richer MDX body copy plus a complete `related_pages` list is enough to surface all Must Launch routes without adding homepage-only layout logic.
- 2026-04-11: The smallest homepage CTA telemetry fix lives in `src/components/CtaBlock.astro`, where non-canonical CTA clicks can emit the same lightweight `dataLayer`/`gtag`/`CustomEvent` pattern already used by the `/try-happyhorse/` generator flow while preserving shared canonical/schema helpers.
- 2026-04-11: Task 7 explainer refinement can stay limited to `src/content/pages/what-is-happyhorse.mdx`; the shared shell already provides the hero, fact-status callout, key facts, FAQ, disclaimer, breadcrumbs, related links, and `/try-happyhorse/` CTA, so strengthening `answer_summary`, key facts, FAQ copy, and body sections is enough to make the first screen answer the query directly while keeping limited, unknown, and mixed wording visible where needed.
- 2026-04-11: Task 8 can keep the comparison work narrow by strengthening only `src/content/pages/happyhorse-vs-seedance.mdx`, adding a content-schema guardrail in `src/content.config.ts`, and extending `tests/e2e/run.mjs` so the canonical comparison page, onward CTA path, and forbidden mirrored route absence are all enforced at build and smoke-test time.
- 2026-04-11: Task 9 can stay narrowly scoped by tightening `src/content/pages/try-happyhorse.mdx` plus the helper copy in `src/components/GeneratorEntry.astro`; that is enough to separate verified public access uncertainty from what this site provides directly without changing the generator logic.
- 2026-04-11: Browser QA on local preview confirmed `/try-happyhorse/` now renders explicit public-vs-site distinction, visible FAQ/disclaimer/alternative links, one successful mock generation, one failure state that does not decrement the free count, and limit-reached visibility after the second successful generation.
- 2026-04-11: Task 10 works best when `/happyhorse-alternatives/` stays an alternatives-first redirection page in `src/content/pages/happyhorse-alternatives.mdx`, while the shared `see_alternatives` CTA in `src/lib/page-shell.ts` sends that route onward to `/try-happyhorse/` rather than broadening into `/best-ai-video-models/`; the comparison and access journeys should stay visible as body links and related links instead of competing primary CTAs.
- 2026-04-11: Task 11 can stay narrow by refining only `src/content/pages/happyhorse-prompts.mdx` and extending `tests/e2e/run.mjs`; the shared `get_prompts` CTA already routes `/happyhorse-prompts/` cleanly into `/try-happyhorse/`, so the missing work was creator-intent copy, HappyHorse-style non-official framing, prompt examples, and route-specific smoke coverage.
- 2026-04-11: Task 12 can stay narrow by refining only `src/content/pages/happyhorse-github.mdx` and extending `tests/e2e/run.mjs`; the shared shell already supplies the unknown-status callout, FAQ, disclaimer, related links, and `/try-happyhorse/` CTA, so the missing work was repo-status-specific copy that separates verified GitHub search intent from unknown official-repo status and clearly distinguishes the route from `/happyhorse-open-source/`.
- 2026-04-11: Task 13 can stay narrow by refining only `src/content/pages/happyhorse-open-source.mdx` and extending `tests/e2e/run.mjs`; the shared shell already supplies the unknown-status callout, FAQ, disclaimer, related links, and alternatives CTA, so the missing work was open-source-specific copy about code, weights, and licensing that stays materially distinct from the repo-status page.
- 2026-04-11: Task 14 can stay narrow by refining only `src/content/pages/best-ai-video-models.mdx` and extending `tests/e2e/run.mjs`; the shared `see_alternatives` CTA already keeps the category route in a support role by sending visitors to `/happyhorse-alternatives/`, so the missing work was category-level copy that clearly routes people back into `/happyhorse-vs-seedance/`, `/happyhorse-alternatives/`, and `/try-happyhorse/` without turning the page into the campaign hub.
- 2026-04-11: Task 15 integrity pass works best with two small guardrails: the homepage should expose the three Should/support routes as visible support links in addition to the five Must routes, and `src/lib/page-shell.ts` should throw if a `related_pages` entry points at a missing route so link-graph drift fails at build time instead of disappearing silently.
- 2026-04-11: The final route-integrity smoke checks now verify the rendered canonicals, breadcrumb output, sitemap membership, and minimum related-link counts for every content route, which turns the final inventory into an explicit build artifact rather than an assumption spread across MDX packets.
- 2026-04-11: Task 16 can stay implementation-light by keeping reusable framework artifacts under `docs/trend-framework/`: one scoring rubric with the locked 30 / 25 / 20 / 15 / 10 weights, one MDX page template that mirrors `src/content.config.ts`, and one Day 3 / 7 / 14 / 30 review checklist. A future trend page can reuse the existing shared content-route architecture by copying the template into `src/content/pages/` without adding new page components.
- 2026-04-11: Final fact-status/compliance polish did not require route-content rewrites; all nine content entries already satisfied `source_urls`, `fact_status`, `last_updated_at`, disclaimer, and visible mixed/unknown wording requirements. The only required fix was to expose stronger CTA telemetry context on both shared CTA links and the `/try-happyhorse/` generator button.
- 2026-04-11: The smallest sitemap-compliance repair is a dedicated static `src/pages/sitemap.xml.ts` alias plus `robots.txt` pointing at `/sitemap.xml`; this preserves the generated Astro sitemap files while exposing the conventional entry path expected by reviewers and crawlers.
- 2026-04-11: The least invasive clean-checkout reliability fix for this repo is to let `tests/e2e/run.mjs` self-build when `dist/index.html` is missing, instead of adding heavier test tooling or changing the current smoke-test architecture.
- 2026-04-11: Generator result URLs should be normalized through `new URL(...)` and rendered only when the protocol is `http:` or `https:`; unsafe or invalid values should remove the anchor `href` entirely and fall back to non-clickable explanatory text.
- 2026-04-11: The cleanest no-Playwright final-review fix on this macOS machine is to keep `tests/e2e/run.mjs` as the single entry point, start `astro preview`, and drive the installed local Chrome through the DevTools Protocol. That gives `npm run test:e2e` a real browser engine for route checks and `/try-happyhorse/` generator interaction while preserving the existing dist-level assertions as a second verification layer.
