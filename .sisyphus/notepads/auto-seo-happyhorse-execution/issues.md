
- 2026-04-11: External Astro implementation-pattern research is still pending; defer Task 1 delegation until the librarian result arrives so stack defaults and config shape are grounded in current guidance.
- 2026-04-11: Task 1 must set `site` early or later canonical/sitemap validation will be polluted by localhost URLs.
- 2026-04-11: Task 1 should create a placeholder root route so preview/HTTP verification has a stable success target before content tasks begin.
- 2026-04-11: User added a new requirement after planning: the site contains a small app where users can generate one or two videos. This may invalidate the current static-only / lead-capture-only architecture and blocks blind execution until the app shape is clarified.
- 2026-04-11: User requested browser-related verification should use `agent-browser` rather than Playwright. Future delegations should reflect that preference when browser QA is needed.
- 2026-04-11: Planning now assumes the existing generation API can be called directly from the frontend. This preserves the user constraint, but downstream implementation must watch for CORS, public-safe credential exposure, quota abuse, and weak rate-limiting risk.
- 2026-04-11: Astro 6.1.5 currently reports deprecation hints for the `z` re-export used in `src/content.config.ts`; Task 1 still builds cleanly with 0 errors, but a later content-model task should switch to Astro's preferred schema import once the target pattern is locked.
- 2026-04-11: The first Task 1 pass still violated the browser-tooling preference by bundling Playwright dependencies and files even though the plan only required a non-locked QA hook; this was corrected by removing the Playwright artifacts entirely.
- 2026-04-11: Once Task 3 added the explicit content loader, the Task 2 MDX packets surfaced hidden YAML issues: every `disclaimer_text` and `last_updated_at` value needed quotes before the shared shell could build against the collection.
- 2026-04-11: Frontend-direct API usage for Task 4 preserves the user constraint but remains a soft-limit model only; localStorage-based 2-free-generation enforcement is bypassable and should be treated as UX gating, not abuse prevention.
- 2026-04-11: The librarian research task returned no usable text output, so Task 4 should rely on the confirmed local insertion points and the Oracle recommendation rather than waiting for additional external guidance.
- 2026-04-11: Browser-agent QA worked against the local preview, but the CLI’s higher-level label actions were inconsistent on the fresh session; ref-based snapshots (`snapshot -i` + `@e*`) were the reliable path for final state verification.
- 2026-04-11: Task 8 browser QA against local preview showed one unrelated console error, `GET /favicon.ico 404`, while the comparison page content, CTA flow, and mirrored-route absence all verified correctly.
- 2026-04-11: Task 9 browser QA against `/try-happyhorse/` still shows the same unrelated preview-console error, `GET /favicon.ico 404`; access copy, disclaimer/FAQ/related-links, and generator success/failure/limit states all verified correctly otherwise.
- 2026-04-11: Task 10 verification can run cleanly through build and smoke tests, but workspace LSP coverage still does not include `.mdx`, so route-packet validation for `src/content/pages/happyhorse-alternatives.mdx` depends on Astro build/check plus browser QA rather than editor diagnostics.
- 2026-04-11: Task 11 browser QA against `/happyhorse-prompts/` confirmed the creator-intent structure and CTA flow, but local preview still logs the same unrelated console error, `GET /favicon.ico 404`, while the prompt examples, non-official HappyHorse-style framing, FAQ, disclaimer, and related links all render correctly.
- 2026-04-11: Task 12 browser QA against `/happyhorse-github/` confirmed the repo-status-specific value, verified-vs-unknown handling, FAQ, disclaimer, and `/try-happyhorse/` CTA, but local preview still logs the same unrelated console error, `GET /favicon.ico 404`.
- 2026-04-11: Task 13 browser QA confirmed `/happyhorse-open-source/` now answers open-source intent directly, routes visitors toward alternatives when public release is unverified, and stays visibly different from `/happyhorse-github/`, but local preview still shows the same unrelated console error, `GET /favicon.ico 404`, on the open-source route.
- 2026-04-11: Task 14 browser QA against `/best-ai-video-models/` confirmed the category page stays support-only, keeps one `see_alternatives` CTA, and links back into the HappyHorse funnel, but local preview still shows the same unrelated console error, `GET /favicon.ico 404`, on this route as well.
- 2026-04-11: Task 15 browser QA against local preview confirmed the route graph and metadata consistency checks, but the same unrelated local-preview console error remains: `GET /favicon.ico 404`.
- 2026-04-11: Task 16 documentation artifacts cannot use LSP diagnostics in this workspace because no `.md` or `.mdx` language server is configured. Verification should rely on schema/spec comparison plus `npm run build`, which passed cleanly after the new artifacts were added.
- 2026-04-11: Final browser-route inspection still shows the known local preview `GET /favicon.ico 404`, and the persistent browser session can carry over the generator quota state in localStorage, so `/try-happyhorse/` CTA verification should rely on unknown-status copy plus the button telemetry attributes rather than assuming the button text always starts at `Start free generation`.
